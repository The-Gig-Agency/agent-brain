{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "mkdir -p /tmp/npm-cache && export NPM_CONFIG_CACHE=/tmp/npm-cache && npm ci && npm run build"
  },
  "deploy": {
    "startCommand": "npm run start:router-recommend:v1",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
